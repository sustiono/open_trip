<% if user_signed_in? %>
  Welcome <strong><%= current_user.name %></strong>
<% end %>

<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>

<% if user_signed_in? %>
  <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
<% else %>
  <%= link_to 'Become a traveler', new_user_registration_path(type: 'traveler') %>
  <%= link_to 'Become a travel agent', new_user_registration_path(type: 'travel_agent') %>
  <%= link_to 'Sign In', new_user_session_path %>
<% end %>
